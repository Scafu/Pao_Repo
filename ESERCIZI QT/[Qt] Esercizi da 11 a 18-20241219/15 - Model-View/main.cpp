#include <QApplication>
#include "ListViewWidget.h"

int main(int argc, char *argv[]) {
    QApplication app(argc, argv);

    // TODO: Create an instance of ListViewWidget and show it
    ListViewWidget widget;
    widget.show();

    return app.exec();
}
